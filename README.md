# E-Commerce Real-Time Payment Fraud Detection

Business Analytics group project — an end-to-end analytics product:
**synthetic data → EDA → cleaning → model → deployed API + review queue → monitoring.**

Business question: *which incoming transactions are likely fraudulent, and how do
we balance blocking fraud against approving legitimate orders?*

> 🇻🇳 Bản tiếng Việt: [README.vi.md](README.vi.md)

---

## Data

- **Base (required):** PaySim — Kaggle `rupakroy/online-payments-fraud-detection-dataset`.
  Download the CSV into **`data/raw/`** (not committed). 6,362,620 rows, fraud rate
  **0.129%**, fraud occurs **only in TRANSFER & CASH_OUT**.
- **Synthetic context (required extension):** identity (Faker) + risk signals
  (device, address mismatch, failed attempts, IP geo-distance, account age, …),
  generated by `src/synth_context.py`. Every field is documented in
  **`docs/data_dictionary.md`**.

## 📥 Download data from Google Drive

Heavy files are **not** in git. Download from the team Drive and place them:

**🔗 Drive link:** https://drive.google.com/drive/folders/1JAhxTGWeXHifeDl3QinpvAfwxxjkF6oR?usp=sharing

| File on Drive | What to do | Required? |
|---|---|---|
| `archive.zip` (178MB) | Unzip, put `PS_20174392719_1491204439457_log.csv` into `data/raw/` | ✅ **Required** |
| `transactions_context.parquet` (82MB) | Place into `data/processed/` | Optional (skip rebuild) |
| `transactions_clean.parquet` (88MB) | Place into `data/processed/` | Optional |

> Just unzip **`archive.zip`** into `data/raw/` and the whole pipeline runs — the
> two parquet files only save you a rebuild (`python src/build_dataset.py`).

## Setup

```bash
pip install -r requirements.txt
# then download the PaySim CSV into data/raw/
```

## Run the pipeline (from repo root)

| # | Command | Output |
|---|---|---|
| 1 | `python src/build_dataset.py` (`--full` for all 6.36M) | `data/processed/transactions_context.parquet` + preview |
| 2 | `python src/eda.py` | `docs/figures/*.png` + `docs/eda_summary.md` |
| 3 | `python src/cleaning.py` | `data/processed/transactions_clean.parquet` + `docs/cleaning_report.md` |
| 4 | `python src/train_validate.py` | `models/fraud_model.joblib` + metrics / leakage check / threshold |
| 5 | `uvicorn api.main:app --reload` | scoring API → http://127.0.0.1:8000/docs |
| 6 | `streamlit run app/streamlit_app.py` | fraud-analyst review queue |
| 7 | `python monitoring/drift.py` | `monitoring/reports/drift_report.md` + `evidently_drift.html` |

## Module → file map

| Module | Files |
|---|---|
| M1 Business understanding & data generation | `src/synth_context.py`, `src/build_dataset.py`, `docs/data_dictionary.md` |
| M2 EDA | `src/eda.py` |
| M3 Cleaning | `src/cleaning.py` |
| M4 Feature engineering | `src/features.py` |
| M5 Model development | `src/train_validate.py` |
| M6 Deployment | `api/main.py`, `app/streamlit_app.py` |
| M7 Monitoring | `monitoring/drift.py` |
| M8 Report & presentation | _TODO_ |

## Key results (15% stratified sample, real prevalence 0.129%)

- **Model:** XGBoost, **AUC-PR 0.997** (all features).
- **Synthetic layer validated — no leakage:** strongest single synthetic feature
  AUC 0.79, synthetic-only model AUC 0.88 (high but < 1.0).
- **Note:** PaySim's balance-reconciliation features are near-deterministic, so
  the toy problem is very easy. An optional **"realistic" scenario** (drop those
  features → `groups="realistic"` in `features.py`) gives a genuine
  precision/recall/cost trade-off if the team wants a richer analysis.
- All tunable assumptions live in `src/config.py` (`SEED`, cost weights) and
  `GEN` in `src/synth_context.py`.

## Status

- [x] **P1** — data generation, data dictionary, EDA, cleaning
- [x] **P2 (draft)** — features, model comparison, leakage check, cost threshold
- [x] **P3 (draft)** — scoring API, review-queue UI, drift monitoring
- [ ] Deploy to a cloud free tier (Hugging Face Spaces / Render) — **live link required**
- [ ] Full-data build + imbalance experiments (SMOTE vs class weights)
- [ ] M8 final report + slides
- [ ] Fill in submission milestone dates
